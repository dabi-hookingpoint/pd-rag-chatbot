# PRD: 후킹포인트 문의 안내 RAG 챗봇

- 작성자: 선다비 (기획PD)
- 프로젝트: Main Quest 3 — 내 도메인에서 더 잘 대답해주는 RAG 챗봇
- 버전: v0.7 (하이브리드 검색: BM25 + Gemini 임베딩 코사인 유사도)

## 0. 변경 이력

- v0.1: 내부용(지원사업)/외부용(IP개발+편집) 2챗봇 분리안
- v0.2: 내부/외부 배치를 반대로 수정, 외부용을 "후킹포인트 소개" 중심으로 재설계
- v0.3: **2챗봇 분리 구조를 폐기하고, 단일 챗봇 + 6개 문의 대분류 탭으로 전면 재설계.** 실제 후킹포인트에 문의가 들어올 때의 자연스러운 분류(제작/기획/후반문의/편집/AI작품/후킹포인트가궁금해요)를 그대로 카테고리 구조로 채택.
- v0.4: 첫 작동 프로토타입(Vite + TS, BM25 검색 + Gemini BYOK) 구현. 대표 지시로 "제작" 카테고리에서 정부·공공 제작지원사업 내용을 전부 삭제하고 후킹포인트 자체 라인업 정보만 남김. 앱 UI에 실제 회사 파비콘을 브랜드 마크로 적용.
- v0.5: **"AI 작품" 카테고리 삭제**(공개 사례가 없다는 사실 자체는 후반문의 코퍼스에 남겨둠), 카테고리 순서를 제작·기획·편집·후반문의·후킹포인트가궁금해요로 재배치. "제작"은 홈에서 누르면 **라인업 문의 / 제작 협업문의** 두 갈래로 갈라지는 선택 모달을 먼저 보여주도록 변경. 각 카테고리 채팅 화면에 우측 정렬 추천 질문 pill을 상시 노출해, 홈 화면의 "클릭으로 진행하는" 상호작용을 채팅 안에서도 유지.
- v0.6: GitHub Pages 배포 완료. 배포 검증 과정에서 `gemini-2.0-flash` 서비스 종료(404)와 `gemini-3.6-flash`의 일일 20건 무료 한도를 발견해 `gemini-flash-lite-latest`로 교체(응답 속도도 함께 개선). 37문항 평가셋을 top-k=5/2로 실제 Gemini API 실행해 결과표 작성 — 상세는 [eval/RESULTS.md](../eval/RESULTS.md).
- v0.7: BM25 키워드 검색을 하이브리드로 업그레이드. 브라우저 ML 모델(transformers.js) 대신 `gemini-embedding-001` API로 청크는 사전 계산(정적 파일)·질문은 실시간 임베딩하는 방식을 채택. 정적 벡터스토어 다운로드 진행률+캐시 UI, 설정 탭 코사인/BM25 가중치 슬라이더 추가. BM25-only vs 코사인-only로 동일 질문을 비교해 실제로 다른(더 의미 기반의) 청크가 검색되는 것을 확인.

## 1. 문제 정의

후킹포인트(영화/드라마 제작사)에 문의하려는 사람들은 실제로는 서로 다른 종류의 질문을 한다 — "이런 제작지원사업에 지원할 수 있나요?"부터 "이 작품 감독이 누구예요?"까지. 이 챗봇은 문의 유형을 6개 대분류로 나누고, 각 대분류에 맞는 공개 자료를 근거로 답한다. 근거가 있으면 출처와 함께 답하고, 근거가 없거나 약하면(예: 아직 공개되지 않은 IP 프로젝트 상세) 지어내지 않고 정직하게 한계를 안내하며 문의 연락처로 유도한다.

## 2. 타겟 유저

후킹포인트에 관심이 있거나 문의하려는 외부인 전체 — 잠재 협업사, 원작 IP 보유자(웹툰/웹소설), 배급사·공동제작사, 구직 희망 스태프(편집 등), 정부 지원사업 정보를 찾는 동종업계 종사자, 그리고 그냥 회사가 궁금한 사람. 완전 공개, 로그인 불필요.

## 3. 핵심 기능 (MVP)

1. **카테고리 탭 UI**: 제작 / 기획 / 편집 / 후반 문의 / 후킹포인트가 궁금해요 — 탭 선택 시 해당 카테고리로 검색 범위가 좁혀짐(전체 검색도 가능). "제작"은 탭 진입 전에 라인업 문의/제작 협업문의 선택 모달을 먼저 띄운다.
2. **하이브리드 검색**: 코사인 유사도(임베딩) + BM25(키워드) 가중합, 선택된 카테고리 내에서 검색
3. **근거 조립**: 검색된 청크의 `id`·`url`·`section`·`source`를 프롬프트에 보존, 오늘 날짜를 시간 컨텍스트로 주입(지원사업 마감일 판단용)
4. **답변 생성**: 사용자 BYOK API 키(Gemini 기본/OpenAI 선택)로 스트리밍 생성 + 취소 버튼
5. **출처 공개**: 답변 하단 출처 칩 → 클릭 시 원문 인용 구간+URL을 보여주는 근거 보기 모달
6. **약한 근거 처리 규칙**: 유사도 임계치 미만이거나 코퍼스에 없는 내용이면 "확인되지 않음"을 명시하고, 상황에 맞는 다음 행동을 안내 (예: 문의 이메일 안내)
7. **출처 성격 라벨**: 청크 출처를 3종 배지로 구분 표시 — "후킹포인트 공식"(사이트 게시 콘텐츠, works.json/contact.astro 등) / "대표 확인"(sources/hookingpoint-facts.md, 사이트에 아직 게시되지 않은 대표 확인 1차 정보) / "업계 일반"(KOFIC/KOCCA/MCST 등 외부기관 참고 정보)
8. **자동 판정(LLM-as-Judge)**: 답변 생성 후 근거성·거절 적절성을 채점해 배지로 표시
9. **사람 피드백**: 👍/👎(+선택 별점), 자동판정 배지와 나란히 표시
10. **"자연스러움 ≠ 사실" 구분**: 문장 유창성과 별개로 "출처 확인됨/부분 확인/미확인" 배지 분리 표시
11. **초기 로딩 경험**: 정적 벡터스토어(`public/data/embeddings.json`) 다운로드 시 홈 화면에 진행률 표시, `localStorage` 캐시로 재방문 시 즉시 로드
12. **카테고리별 추천 질문 pill**: 채팅 화면에 진입해도 홈 화면과 동일한 "우측 정렬 클릭형" 패턴을 유지하기 위해, 카테고리마다 미리 정한 추천 질문 2개를 pill로 상시 노출. 클릭하면 바로 그 질문이 전송됨

## 4. 근거 자료 계획 (카테고리별)

WebSearch/WebFetch로 실접근 확인했거나 저장소 내 실제 파일로 확인한 소스만 사용한다. 확인 안 된 항목은 "확인 필요"로 남기고 구현 단계에서 재검증한다.

> **출처 처리 방침(공통)**: 아래 "신규 확인 사실"은 대표(선다비)가 이번 대화에서 확인해 준 실제 회사 정보다. 실제 hookingpoint 사이트(`about.astro`/`ip-development.astro` 등)는 건드리지 않기로 결정했으므로, 이 사실들의 `url`은 이 프로젝트 저장소 내 `sources/hookingpoint-facts.md`(대표 확인 1차 자료, 외부 공개 URL 아님)로 고정한다. 화면에는 일반 외부기관 출처와 다르게 "대표 확인" 배지로 구분 표시한다.

### 4-1. 제작 — 후킹포인트 현재 라인업 (v0.4에서 범위 축소)

> **v0.4 변경**: 대표 지시로 정부·공공 제작지원사업(KOFIC/KOCCA/지역영상위) 관련 내용을 이 카테고리에서 전부 삭제했다. "제작" 탭은 더 이상 "누구나 쓰는 지원사업 검색 도구"가 아니라, **후킹포인트 자신이 지금 무엇을 제작하고 있는지**에 답하는 탭으로 범위를 좁힌다. (조사했던 KOFIC/KOCCA/지역영상위 소스는 `sources/hookingpoint-facts.md`와 별개로 리서치 기록에는 남아있지만 현재 코퍼스에는 포함하지 않는다.)

- **신규 확인 사실 (회사 1차 정보)**: 현재 예술영화 1편, 저예산 상업영화 1편, 드라마 2편, 숏폼 드라마 1편을 준비 중 (개별 프로젝트 상세는 비공개, 이 라인업 개요만 공개 대상)
- 보조: `contact.astro` — 제작 협업 문의는 이메일로

**한계**: 근거 자료가 라인업 개요 한 줄 + 연락처뿐이라 매우 얇다. "지금 뭐 만들고 있어요?"류 질문에는 답할 수 있지만, 그 이상의 세부 질문(개별 작품명, 캐스팅 등)은 전부 비공개로 정직하게 안내해야 한다.

**하위 진입점 2개로 분리 (v0.4.1)**: 홈 화면에서 "제작"은 더 이상 하나의 진입점이 아니라 두 개로 나뉜다.
- **라인업 문의** — 위 코퍼스를 근거로 채팅 응답 (기존 "제작" 탭과 동일한 RAG 흐름)
- **제작 협업문의** — 채팅이 아니라, 전화번호·이메일이 적힌 연락처 모달을 바로 띄움 (검색/생성 없음)
- **미해결**: 회사 전화번호를 아직 받지 못해 모달에는 이메일만 활성화되어 있고 전화 칸은 "확인 중"으로 표시된다. 전화번호를 받으면 `src/types.ts`의 `PRODUCTION_CONTACT.phone`에 채워 넣으면 즉시 반영된다.

### 4-2. 기획 — IP 개발 + 현재 라인업 (업계자료로 보완)
- `ip-development.astro` — "IP 프로젝트 상세는 관리자 열람권한 회원만 확인 가능, 문의는 이메일로" 정책 문구 (회사 1차 소스, 짧지만 정확)
- `ip-projects.json` — **전 항목이 placeholder**("프로젝트 A 제목 작성 예정" 등) → RAG 코퍼스에서 제외. 이 프로젝트들의 시놉시스/장르를 묻는 질문에는 "비공개, 문의 안내"로 정확히 거절하는 것이 정답
- **신규 확인 사실 (회사 1차 정보, 4-1과 공유)**: 현재 예술영화 1편, 저예산 상업영화 1편, 드라마 2편, 숏폼 드라마 1편 준비 중이라는 라인업 개요는 공개 가능한 사실 → "지금 어떤 프로젝트를 개발 중이세요?" 질문에 이 요약으로 답하고, 개별 제목/시놉시스는 비공개로 안내
- 업계일반 보완: 문체부 웹툰 표준계약서 6종+영상화 판권 계약서 2종, 웹소설 표준계약서 3종, 영화투자 표준계약서, 콘진원 2024 웹툰산업 실태조사 수치, KOMACON 웹툰 IP 창업지원센터 소개

**한계**: 표준계약서 해설집 PDF 상세, 콘진원 실태조사 원문 PDF 직링크는 구현 단계 재확인 필요.

### 4-3. 후반 문의 — 후반작업 전반 + VFX 협업 + AI 작업 파이프라인
- `contact.astro` — "편집 의뢰, IP 자료 요청, 협업 제안 모두 환영" + 연락처
- **신규 확인 사실 (회사 1차 정보)**: 사외이사 동은철(VFX 슈퍼바이저 출신, 20년 이상 경력)의 소속사 "원컨텐츠"와 협업사 관계 — VFX 관련 문의에 실제 협업 네트워크로 답변 가능
- **신규 확인 사실 (회사 1차 정보)**: AI 작업 의뢰 시 진행 공정 — 기획 → 스토리보드 → 3D 작업(현장 비용 절감) → 작업물 익스포트
- 업계일반 보완: 문체부 영화 근로표준계약서(편집 포함 전 직군), KOFIC 영화스태프 표준근로계약서 발표 공지, 방송영상프로그램 제작스태프 표준계약서 3종, KOFIC 영화산업 안전보건체계 연구(국회도서관 소장)

**한계**: 편집·DI·사운드믹싱 등 후반작업 "공정" 자체를 상세 규정한 공식 공개 문서는 여전히 부족 — 다만 위 VFX 협업·AI 작업 파이프라인 신규 사실 덕분에 "당신 회사 후반작업 능력이 뭐예요?"류 질문에는 이제 실제 근거로 답할 수 있게 됨. 세부 공정 규격을 묻는 질문(예: "몇 프레임 단위로 작업하나요")은 여전히 자료 범위 밖으로 정직하게 거절.

### 4-4. 편집 — 필모그래피 + 부가 서비스 (근거 풍부, 회사 1차 데이터)
- `works.json` — 실제 10개 작품, 각 `title/director/cast/genre/synopsis/type/releaseYear/productionCompany/writer/watchLink` 보유 (예: "메리 베리 러브"-CJ ENM·닛폰테레비/감독 김수정·노영섭, "지구 최후의 여자"-씨네21 링크 등)
- `filmography.astro`("EDITING : 작품별 편집 로그" 페이지) + 작품별 `editing.astro` 편집 로그 페이지 — 현재 `editingApproach`는 placeholder
- **신규 확인 사실 (회사 1차 정보)**: 편집 외 제공 서비스 — 시나리오 피드백, 요청 시 스토리보드 작업 주기적 참여, 종합편집(종편) 참여
- 업계일반 보완: 편집 스태프 표준근로계약서(위 4-3과 공유)

**한계**: `editingApproach`(편집 포인트) 필드가 전 작품 placeholder → 편집 스타일/기법 세부 질문은 현재 답변 불가, "아직 공개된 정보 없음"으로 정직 답변.

> **v0.5 변경**: 별도 "AI 작품" 카테고리는 삭제했다. "AI로 만든 작품" 포트폴리오 데이터가 저장소에 없어(내부 AI 제작 도구 `ip-development-tools.astro`는 작품 사례가 아님) 카테고리 하나를 채울 근거가 되지 못한다고 판단했기 때문이다. 대신 "AI 작업 파이프라인 및 현재 공개 사례 없음" 사실은 4-3(후반 문의) 코퍼스에 그대로 남겨, 관련 질문이 들어오면 후반 문의 탭에서 정직하게 답한다.

### 4-5. 후킹포인트가 궁금해요 — 회사 일반 정보
- `about.astro` — **현재 placeholder 상태**("회사 소개 문단 작성 예정"). 대표가 스토리(편집실 경험이 어떻게 IP기획으로 이어졌는지)를 채워야 실질적 소스가 됨 → 이번 대화에서는 미확보, 후속 확인 필요
- `contact.astro` (연락처, 협업 문의 안내)
- `ip-development.astro` 정책 문구
- `works.json` 요약(몇 작품, 어떤 장르 등 개괄 질문용 — 편집 탭과 코퍼스 공유)

**한계**: About 페이지 내용이 아직 없어 "후킹포인트는 어떤 회사인가요?"류 정체성 질문은 현재 연락처/필모그래피 요약 정도로만 답 가능. 대표의 회사 소개 스토리를 후속으로 받아야 완성도가 올라간다.

### 청크 스키마 (공통)

```json
{
  "id": "hookingpoint-editing-merry-berry-love",
  "category": "편집",
  "source": "후킹포인트",
  "title": "메리 베리 러브",
  "section": "작품정보",
  "url": "https://hookingpoint.example/filmography#merry-berry-love",
  "text": "...",
  "deadline": null,
  "fetched_at": "2026-08-31"
}
```

```json
{
  "id": "kofic-production-2026-ai-longform-001",
  "category": "제작",
  "source": "KOFIC",
  "title": "2026년 AI 기반 영화 제작지원(장편)",
  "section": "지원대상",
  "url": "https://www.kofic.or.kr/kofic/business/prom/promotionBoardDetail.do?seqNo=17733",
  "text": "...",
  "deadline": "2026-03-03",
  "fetched_at": "2026-08-31"
}
```

```json
{
  "id": "hookingpoint-facts-vfx-partnership-001",
  "category": "후반문의",
  "source": "대표확인",
  "title": "VFX 협업 관계",
  "section": "협업사",
  "url": "internal://sources/hookingpoint-facts.md#vfx-partnership",
  "text": "사외이사 동은철(VFX 슈퍼바이저 출신, 20년 이상 경력)의 소속사 원컨텐츠와 협업사 관계.",
  "deadline": null,
  "fetched_at": "2026-08-31"
}
```

- `id` 규칙: `{source-slug}-{category-slug}-{연도/회차 또는 작품id}-{순번}`
- `category` ∈ {제작, 기획, 후반문의, 편집, 회사정보}
- `deadline`은 제작(지원사업) 카테고리 전용
- `source` ∈ {"후킹포인트"(사이트 게시), "대표확인"(sources/hookingpoint-facts.md, 미게시 1차 정보), 그 외 기관명(업계일반)} — 화면 3종 배지 구분의 근거. `source: "대표확인"`인 청크의 `url`은 `internal://` 접두사로 표시해 외부 URL과 구분한다

## 5. 화면 구성

1. **메인 챗 화면 (`/`)**: 상단에 5개 카테고리 탭, 탭 아래 채팅 UI. 탭 전환 시 검색 범위(카테고리 필터)가 바뀌고, 안내 문구가 탭별로 다르게 노출. 채팅 화면에는 카테고리별 추천 질문 pill이 항상 함께 보인다
2. **탭별 딥링크 CTA 버튼** (답변과 별개로 탭 상단/하단에 고정 노출):
   - 편집 탭: "실제 과정 살펴보기" → 후킹포인트 사이트 EDITING(필모그래피) 페이지로 이동 / "편집 의뢰하기" → 연락처 페이지(이메일)로 이동
   - 기획 탭: "IP 열람 문의하기" → 연락처 페이지로 이동
   - 후반 문의 탭: "AI 작업 의뢰 문의" / "VFX 협업 문의" → 연락처 페이지로 이동
   - 후킹포인트가 궁금해요 탭: "회사 소개 더 보기" → About 페이지로 이동
3. **답변 영역**: 답변 텍스트 + 출처 칩(회사정보/업계일반 배지 포함) + 판정 배지 + 피드백 버튼
4. **근거 보기 모달**: 출처 칩 클릭 시 원문 인용 구간 + URL
5. **설정 패널**: API 키 입력(로컬 저장), 엔진 선택(Gemini/OpenAI), 검색 가중치(코사인 vs BM25) 슬라이더
6. **초기 로딩 화면**: 벡터스토어(`embeddings.json`) 다운로드 진행률
7. **실험/평가 리포트 페이지 (`/eval`)**: 카테고리별 질문 결과, 세팅 변경 성능 비교표, 실패 사례 목록

## 6. 기술 스택 & 아키텍처

- 프레임워크: Vite + TypeScript, 정적 빌드 → GitHub Pages
- 임베딩: Gemini `gemini-embedding-001`(768차원). **v0.7에서 계획 변경** — 브라우저 ML 모델(transformers.js) 대신, 사용자가 이미 입력하는 Gemini API 키를 그대로 재사용해 (1) 청크는 `scripts/build_embeddings.py`로 사전 계산해 정적 파일로 배포, (2) 질문만 런타임에 같은 모델로 실시간 임베딩. 브라우저에 ML 런타임(WASM/ONNX)을 심지 않아도 되고, BYOK 키 하나로 생성·판정·검색을 전부 처리
- 벡터스토어: `public/data/embeddings.json`(~320KB, 사전 계산) — 첫 방문 시 다운로드+진행률 표시, `localStorage` 캐시
- 키워드 검색: 클라이언트 BM25 (한글 문자 bigram 토크나이저)
- 하이브리드 결합: BM25 점수를 카테고리 내 최댓값으로 정규화(0~1) 후 코사인 유사도와 가중합. 가중치는 설정 탭 슬라이더로 조절(기본 0.5). 결합 점수가 임계치(0.12) 미만이면 근거 없음으로 처리
- 생성/판정 엔진: 사용자 BYOK API 키로 클라이언트 직접 호출 (로컬 저장, 서버 전송 없음)
- 배포: GitHub Pages, 별도 GitHub 저장소

## 7. 판정(Judge) & 피드백 설계

- 자동 판정: 답변+검색된 청크 원문을 입력으로 "근거성(0~1)", "거절 적절성(y/n)", "미근거 진술 포함 여부"를 JSON으로 반환
- 사람 피드백: 👍/👎(+선택 별점 1~5) + 자유 코멘트
- "자동 판정: 근거성 0.8, 거절 적절함" + "사람 평가: 👍 3 / 👎 1"을 나란히 표시

## 8. 실험 & 평가 계획

카테고리별 평가 질문셋(총 약 37문항):
- 제작 5문항 (라인업 개요 질문, 개별 프로젝트 상세 질문은 "비공개 거절"이 정답)
- 기획 8문항 (IP 프로젝트 상세 질문 3개는 "비공개 거절"이 정답, 표준계약서 질문 5개는 업계일반 라벨 필수)
- 편집 10문항 (필모그래피 사실 질문, 근거 풍부 — 정답률 목표 높게)
- 후반문의 9문항 (VFX 협업·AI 작업 파이프라인 질문은 근거 있음, 공정 세부질문은 "자료 범위 밖" 거절이 정답)
- 회사정보 5문항 (연락처/협업방법 등)

루브릭(직접 설계): 근거 일치도, 사실 정확성, 거절 적절성, 완전성 — 4항목 각 0~2점. 여기에 "출처 라벨링 정확성"(후킹포인트 vs 업계일반) 항목 추가. 실제 채점은 4항목을 세분화하는 대신 judge의 `groundedness`(0~1)를 근거일치도+사실정확성 대리지표로, `refusal_appropriate`를 거절적절성 대리지표로, 답변 텍스트의 "업계 일반" 라벨 포함 여부를 라벨링 정확성으로 자동 판정했다(자세한 채점 규칙: [eval/analyze.py](../eval/analyze.py)).

실험 변수: top-k(근거 개수)를 5→2로 낮춰 비교 실행(v0.6, BM25-only 시점). **v0.7에서 하이브리드 검색 도입** — 같은 37문항을 코사인 가중치별로 재실행하는 것은 다음 실험으로 남김(청크 크기 실험은 코퍼스가 작아 우선순위 낮춤). 결과는 [eval/RESULTS.md](../eval/RESULTS.md)에 표로 기록.

**실행 결과 (v0.6, 실제 Gemini API로 37문항 전부 실행)**:

| top-k | 통과율 |
|---|---|
| 5 (기본값) | **34/37 (92%)** |
| 2 | 31/37 (84%) |

top-k를 낮추면 특히 기획 카테고리(100%→75%)에서 성능이 떨어져 **top-k=5를 기본값으로 확정**했다. 실패 사례 3건을 직접 읽어보니 챗봇 자체는 전부 정확히 답했고, 원인은 평가 스크립트의 채점 규칙(부분 거절 패턴을 인식 못함)이었다 — 실패 원인 단계 분류: 검색 1건(top-k 부족), 판정 3건(채점 규칙 미비), 생성 0건. 상세는 RESULTS.md 참고.

## 9. 배포 & 수용 기준

- 배포: [dabi-hookingpoint/pd-rag-chatbot](https://github.com/dabi-hookingpoint/pd-rag-chatbot) (GitHub Pages) — **https://dabi-hookingpoint.github.io/pd-rag-chatbot/** 에서 실제 접속 가능. README에 API 키 발급 안내(Gemini 무료 티어) 포함
- 수용 기준 재확인 결과 (37문항 실행, top-k=5 기본 배포 세팅 기준):
  - 편집 탭: 대표 질문 10개 중 9개 정확한 출처 답변 → **기준(8개 이상) 충족**
  - 기획/제작 탭: "비공개"가 정답인 개별 프로젝트 질문 전부 정확히 거절(자동채점상 3건이 "실패"로 잡혔지만 실제 답변 확인 결과 전부 정직하게 거절함) → **충족**
  - 모든 업계일반 답변에 라벨 누락 없음(기획 4문항·후반문의 3문항 전부 "업계 일반" 문구 포함 확인) → **충족**

## 10. 확장 아이디어 (스트레치)

- 후킹포인트가 `editingApproach`·IP 시놉시스·AI 작품 사례를 실제로 채워 넣으면, 그때 다시 "AI 작품" 같은 카테고리를 되살릴지 검토하는 재수집 파이프라인
- 엔진 비교(Gemini vs 다른 API), 쿼리 확장·리랭킹
- 사람 피드백 누적 → 실패 사례 기반 프롬프트/검색 개선 사이클
- 모바일 반응형, 근거 보기 모달 UX 디테일
